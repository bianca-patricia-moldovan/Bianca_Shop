<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Bianca_Shop.Views.UserPage">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Username" Text="{Binding Username}" HeightRequest="100" />
            <Editor Placeholder="Password" Text="{Binding Password}" HeightRequest="100" />
            <Editor Placeholder="Address" Text="{Binding Address}" HeightRequest="100" />
            <Editor Placeholder="Phonenumber" Text="{Binding PhoneNumber}" HeightRequest="100" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Save" Clicked="OnSaveButtonClicked" />
                <Button Grid.Column="1" Text="Delete" Clicked="OnDeleteButtonClicked"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>